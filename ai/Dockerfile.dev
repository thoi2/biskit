# 1. 베이스 이미지 설정
FROM python:3.10-slim

# 2. 작업 디렉토리 설정
WORKDIR /ai_app

# 3. 의존성 파일 복사 및 설치
# 소스 코드는 docker-compose.override.yml에서 볼륨으로 마운트될 예정입니다.
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4. 포트 노출
EXPOSE 8000

# 5. 애플리케이션 실행 (핫 리로딩 활성화)
# --reload 플래그를 추가하여 코드 변경 시 서버가 자동으로 재시작됩니다.
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
